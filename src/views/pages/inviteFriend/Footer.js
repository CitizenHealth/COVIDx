import React from "react";
import {StyledFooter} from './Styles/StyledFooter'

export const Footer = () => {
    return (
        <StyledFooter>
        <h4>
                You'll also receive 10 covids each for each friend your friends invite, then 1 covid for each friend they invite, then 10% at each level below that. 
                The goal is to "Connect, not infect". 
        </h4>
        </StyledFooter>
    )
}